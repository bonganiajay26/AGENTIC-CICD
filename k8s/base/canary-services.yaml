# AUTO-GENERATED by generate_cicd.py
# Argo Rollouts requires a stable + canary Service pair
---
apiVersion: v1
kind: Service
metadata:
  name: my-app-stable
  labels:
    app: my-app
    role: stable
spec:
  selector:
    app: my-app
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: my-app-canary
  labels:
    app: my-app
    role: canary
spec:
  selector:
    app: my-app
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
